# Copy this file to .env and update the values before running.
# Only SECRET_KEY and JWT_SECRET are required for local deployment.
# ANTHROPIC_API_KEY is optional and only needed if you enable AI API calls.

SECRET_KEY=change-this-to-a-secure-value
JWT_SECRET=change-this-jwt-secret
DEBUG=false
ANTHROPIC_API_KEY=
